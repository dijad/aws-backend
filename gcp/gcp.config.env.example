# Opcional — los scripts gcp también leen backend/.env (recomendado).
# Copia solo si prefieres separar credenciales GCP: cp gcp.config.env.example gcp.config.env

GCP_PROJECT_ID=
GCP_REGION=
CLOUD_SQL_INSTANCE=

DB_NAME=aws
DB_USER=app_user
